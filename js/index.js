$('document').ready(() => {

    // TRANSITO
    $('#162').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 0;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 5;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}162, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#181').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 0;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 5;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}181, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#244').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 0;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 1.5;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}244, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#102').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 10;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 8;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}102, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });
    
    $('#108').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 0;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 6;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}108, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#165').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 5;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 10;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 20;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}165, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#173').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 5;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 10;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}173, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });
    
    $('#50').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 0;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 150;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}50, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });
    
    $('#54').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 0;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 5;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}54, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#53').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 0;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 100;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}53, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#65').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 0;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 20;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}65, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    // CRIMES
    $('#02').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 0;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 5;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}02, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#287').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 5;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 10;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 20;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}287, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#342').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 15;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 20;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 40;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}342, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#92§1').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 0;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 7;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}92§1, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#19').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 0;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 5;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}19, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#331').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 10;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 15;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}331, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#330').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 15;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 10;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}330, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#171').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 25;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 20;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}171, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#158').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 15;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 15;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}158, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#299').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 15;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 7;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}299, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#298').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 5;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 1.5;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}298, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#150').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 15;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 10;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}150, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#520').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 0;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 0;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}520, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)

        alert(`A cada R$ 2.000 em multa será considerado 1 Mês na pena.`)
    });

    // CRIMES NIVEL 01
    $('#129').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 10;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 5;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 10;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}129, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#135').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 10;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 10;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 50;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}135, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#139').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 30;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 20;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 30;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}139, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#147').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 15;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 5;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 20;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}147, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#288').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 20;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 20;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}288, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#358').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 20;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 20;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 40;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}358, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#334').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 15;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 30;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}334, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)

        alert(`Acima de 3 Itens.`)
    });

    $('#334§3').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 10;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 20;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 40;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}334§3, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)

        alert(`Acima de 10 Itens.`)
    });

    $('#56').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 0;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 15;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}56, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    // CRIMES NIVEL 02
    $('#157').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 15;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 20;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 40;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}157, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });
    
    $('#157§2').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 10;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 15;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 30;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}157§2, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#157§3').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 25;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 25;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 40;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}157§3, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#157§4').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 25;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 30;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}157§4, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#180').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 10;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 10;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 20;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}180, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#351').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 15;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 30;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}351, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#06').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 50;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 60;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}06, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#33').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 15;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 0.250;
        const multaAntiga =  $('#multa').html();

        alert(`Acima de 3`)
        const qtdDrogas = prompt(`Quantas unidades de drogas?`);
        const TotalDrogas = qtdDrogas*multa;

        const multaTotal = (Number(multaAntiga) + Number(TotalDrogas));
        const fianca = 25;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}33, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#289').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 0;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 0;
        const multaAntiga =  $('#multa').html();

        alert(`Multar 1,5x o valor que estava sendo portado`)
        const qtdDinheiro = prompt(`Qual o valor de dinheiro sujo estava sendo portado?`);
        const TotalDinheiro = qtdDinheiro*1.5;
        const TotalDinheiro2 = TotalDinheiro/100/10;

        const multaTotal = (Number(multaAntiga) + Number(TotalDinheiro2));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}289, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#155').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 5;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 10;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 20;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}155, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#333').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 15;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 30;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 60;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}333, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#57').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 20;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 25;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}57, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    // CRIMES NIVEL 03
    $('#121§3').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 30;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 50;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}121§3, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)

        alert(`Sem intenção de matar.`);
    });

    $('#121§1').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 60;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 50;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}121§1, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)

        alert(`Com intenção de matar.`);
    });

    $('#129§1').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 40;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 35;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}129§1, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#148').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 50;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 30;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}148, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#148§1').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 60;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 100;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}148§1, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#157§5').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 60;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 50;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}157§5, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#59').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 60;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 60;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}59, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#60').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 60;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 50;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}60, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#61').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 60;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 250;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}61, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#18').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 50;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 60;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}18, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    //Porte de Armamento e colete a prova de balas
    $('#14').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 30;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 50;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}14, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#16§1').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 50;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 50;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}16§1, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#15').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 45;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 50;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}15, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });

    $('#16').on('click', () => {
        const input = $('#TotalArtigos').html();
        const meses = 60;
        const mesesAntigo = $('#meses').html();
        const mesesTotal = (Number(mesesAntigo) + Number(meses));
        const multa = 50;
        const multaAntiga =  $('#multa').html();
        const multaTotal = (Number(multaAntiga) + Number(multa));
        const fianca = 0;
        const fiancaAntiga = $('#fianca').html();
        const fiancaTotal = (Number(fiancaAntiga) + Number(fianca));
        $('#TotalArtigos').html(`${input}16, `)
        $('#multa').html(`${multaTotal.toFixed(3)}`)
        $('#meses').html(`${mesesTotal}`)
        // $('#fianca').html(`${fiancaTotal.toFixed(3)}`)
    });
});

function clean(){
    $('#TotalArtigos').html(' ');
    $('#multa').html(` `);
    $('#meses').html(` `);
    $('#fianca').html(` `);
}

function primario(){
    const multa = $('#multa').html();
    const porcentagemMulta = (multa*25)/100;
    const totalMulta = multa-porcentagemMulta;

    const meses = $('#meses').html();
    const porcentagemMeses = (meses*25)/100;
    const totalMeses = meses-porcentagemMeses;

    $('#multa').html(`${totalMulta.toFixed(3)}`);
    $('#meses').html(`${totalMeses.toFixed(0)}`);
}

function colaborate(){
    const multa = $('#multa').html();
    const porcentagemMulta = (multa*10)/100;
    const totalMulta = multa-porcentagemMulta;

    const meses = $('#meses').html();
    const porcentagemMeses = (meses*10)/100;
    const totalMeses = meses-porcentagemMeses;

    $('#multa').html(`${totalMulta.toFixed(3)}`);
    $('#meses').html(`${totalMeses.toFixed(0)}`);
}

function finalizar(){
    const artigos = $('#TotalArtigos').html();
    const multa = $('#multa').html();
    let meses = $('#meses').html();
    const name = $('#name').val();
    const cpf = $('#cpf').val();
    const rg = $('#rg').val();

    if(meses >= 60){
        meses = 60
    }

    const div = document.createElement('div');
    div.classList.add('finalizado');

    const buttonVoltar = document.createElement('button');
    buttonVoltar.classList.add('voltarFinalizado');

    const buttonCopy = document.createElement('button');
    buttonCopy.classList.add('copiarFinalizado')

    const label1 = document.createElement('label');

    //ATUALIZAÇÃO 0.1 FOI RETIRADO ESSES ITENS
        // const label2 = document.createElement('label');
        // const label3 = document.createElement('label');
        // const label4 = document.createElement('label');
        // const label5 = document.createElement('label');
        // const label6 = document.createElement('label');

    //ATUALIZAÇÃO 0.1 FOI ALTERADO A FORMA DE ESCRITA DESSE ITEM
    label1.innerHTML = `
        Nome: ${name}
        <br>CPF: ${cpf}
        <br>RG: ${rg.toUpperCase()}
        <br>Artigos: ${artigos}
        <br>Pena: ${meses} Meses
        <br>Multa: R$ ${multa}
    `;

    //ATUALIZAÇÃO 0.1 FOI RETIRADO ESSES ITENS
    // label2.innerText = `CPF: ${cpf}`;
    // label3.innerText = `RG: ${rg.toUpperCase()}`;
    // label4.innerHTML = `Artigos: ${artigos}`;
    // label5.innerHTML = `Pena: ${meses} Meses`;
    // label6.innerHTML = `Multa: R$ ${multa}`;
    
    buttonVoltar.innerHTML = `Voltar`;
    buttonCopy.innerHTML = `Copiar`;
    
    //ATUALIZAÇÃO 0.1 FOI ADICIONADO ESSA VARIÁVEL DE FICHA
    const ficha =
        `Nome: ${name} \nCPF: ${cpf} \nRG: ${rg.toUpperCase()} \nArtigos: ${artigos} \nPena: ${meses} Meses \nMulta: R$ ${multa}`;

    //ATUALIZAÇÃO 0.1 FOI ADICIONADO ESSE BOTÃO DE COPIAR A FICHA
    function copy(){
        navigator.clipboard.writeText(ficha)
        .then(() => {
                alert(`Ficha copiada para a área de transferência`)
            })
    }
    
     
    document.body.appendChild(div);

    div.appendChild(label1);

    //ATUALIZAÇÃO 0.1 FOI RETIRADO ESSES ITENS
        // div.appendChild(label2);
        // div.appendChild(label3);
        // div.appendChild(label4);
        // div.appendChild(label5);
        // div.appendChild(label6);
        
    div.appendChild(buttonVoltar);
    div.appendChild(buttonCopy);

    $(buttonVoltar).on('click', () => {
        window.location.href = "./index.html";
    });

    $(buttonCopy).on('click', () => {
        copy()
    });
}